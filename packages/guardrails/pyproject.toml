[project]
name = "fenix-guardrails"
version = "0.1.0"
description = "Sanitización, allowlists, detección de injection, PII (F6)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fenix_guardrails"]
